# 模版详情（inner_bd_quick_cn，可读完整版，不遗漏字段）

- `结果码`: `1`
- `结果信息`: `null`

## 顶层数据字段

| 字段 | 值 |
|---|---|
| `ID` | `inner_bd_quick_cn` |
| `AI类型` | `null` |
| `业务编码` | `bd_ai_report` |
| `创建时间` | `1740453641400` |
| `创建人` | `玄关Ai组` |
| `当前使用` | `null` |
| `删除标记` | `0` |
| `描述` | `简化版药品分析方案，适合初步筛选和快速决策` |
| `目录ID` | `1992887787073851394` |
| `是否生成总结` | `null` |
| `编辑标记` | `1` |
| `启用图片生成` | `0` |
| `语言` | `zh_CN` |
| `模版类型ID` | `inner_deep` |
| `模式` | `quick` |
| `名称` | `BD研究快评报告` |
| `父模版ID` | `null` |
| `人员ID` | `12337` |
| `人员姓名` | `陈洁` |
| `是否公开` | `1` |
| `状态` | `1` |
| `第三方系统` | `null` |
| `更新时间` | `1748248843680` |

## 提示词

```text
你是一个专业的医药研究分析师，请基于以下背景信息进行分析并给出详细的回答。

分析对象：
- 公司名称：{公司名}
- 产品名称：{产品名}

分析要求：
1. 基于公开、可验证的数据来源进行分析
2. 优先引用权威数据库和文献，如：
  - 严格按优先级引用权威来源并标注数据库类型：
    数据来源优先级：
      首要引用：
        - PubMed/Medline
        - FDA/EMA/NMPA官方文件
        - ClinicalTrials.gov
        - 药融云/IQVIA数据
      次级引用：
        - Embase
        - DrugBank
        - 企业年报
        - 行业报告（需注明发布机构）
      禁止使用：
        - 非官方新闻稿
        - 未注明出处的论坛/自媒体内容

    文献与数据引用规范：
      临床试验数据：
        - 必须同时核查ClinicalTrials.gov和中国临床试验登记平台（如涉及国内研发）
      市场分析：
        - 优先引用IQVIA销售数据或药融云中标数据库
        - 需注明统计年份与样本范围
      政策解读：
        - 仅采用FDA/NMPA/CDE官网文件原文
        - 避免间接引用第三方解读

    特殊要求：
      专利信息：
        - 通过WIPO或CAS数据库验证药物分子专利状态
        - 标注专利到期年份
      安全性数据：
        - 若涉及副作用，需关联EMA的EudraVigilance或FDA Adverse Events报告
      图表标注：
        - 所有图表须注明来源（如：数据来源：ClinicalTrials.gov, 2024）
3. 分析原则：
   - 数据严谨，注明数据来源
   - 结论清晰，有理有据
   - 避免推测性表述
   - 如数据存在不确定性，明确指出

4. 内容要求：
   - 结合商机背景信息
   - 保持专业性和准确性
   - 层次分明，逻辑清晰
   - 如信息不足，明确指出信息缺口

请基于以上要求进行分析并提供详细答案。

回答：

```

## 必填上下文

1. `公司名`
2. `产品名`

## 总结提示词

```text
null
```

## 章节列表

- 章节总数: `5`

### 章节 1: 产品综合信息

| 字段 | 值 |
|---|---|
| `名称` | `产品综合信息` |
| `提示词` | 见下方代码块 |
| `doSectionSummary` | `null` |
| `sectionSummaryPrompt` | `null` |

#### 章节提示词

```text

```

#### 问题列表（2）

##### 问题 1.1: 产品标识

| 字段 | 值 |
|---|---|
| `ID` | `4dbee8ae-d82e-4830-9088-e36b227b0d51` |
| `模版ID` | `inner_bd_quick_cn` |
| `章节` | `产品综合信息` |
| `标题` | `产品标识` |
| `场景` | `null` |
| `创建时间` | `1748248843685` |
| `更新时间` | `1748248843685` |
| `是否联网` | `null` |

**数据源**

| 字段 | 值 |
|---|---|
| `自定义数据源ID` | `null` |
| `原始文档` | `null` |
| `来源类型` | `null` |

**内容**

```text
{产品名}的商品名和研发代码是什么？
```

**提示词**

```text
null
```

---

##### 问题 1.2: 公司介绍

| 字段 | 值 |
|---|---|
| `ID` | `38fa789c-59f5-4c3b-8aa2-569c078072ed` |
| `模版ID` | `inner_bd_quick_cn` |
| `章节` | `产品综合信息` |
| `标题` | `公司介绍` |
| `场景` | `null` |
| `创建时间` | `1748248843685` |
| `更新时间` | `1748248843685` |
| `是否联网` | `null` |

**数据源**

| 字段 | 值 |
|---|---|
| `自定义数据源ID` | `null` |
| `原始文档` | `null` |
| `来源类型` | `null` |

**内容**

```text
基于以下背景信息：{机会概述}，分析{公司名}的主营业务、总部位置、技术平台、融资历史、是否上市、上市市值、地点、主要管线产品、管理层基本信息是什么？
```

**提示词**

```text
null
```

---

### 章节 2: 产品力分析

| 字段 | 值 |
|---|---|
| `名称` | `产品力分析` |
| `提示词` | 见下方代码块 |
| `doSectionSummary` | `null` |
| `sectionSummaryPrompt` | `null` |

#### 章节提示词

```text

```

#### 问题列表（1）

##### 问题 2.1: 靶点认可度

| 字段 | 值 |
|---|---|
| `ID` | `d148ac3a-3590-4067-bfd0-7aef7c4493c9` |
| `模版ID` | `inner_bd_quick_cn` |
| `章节` | `产品力分析` |
| `标题` | `靶点认可度` |
| `场景` | `null` |
| `创建时间` | `1748248843685` |
| `更新时间` | `1748248843685` |
| `是否联网` | `null` |

**数据源**

| 字段 | 值 |
|---|---|
| `自定义数据源ID` | `null` |
| `原始文档` | `null` |
| `来源类型` | `null` |

**内容**

```text
靶点机制是什么？同靶点海内外在研产品有哪些，具体列出。上市产品的上市年份、上市国家、获批适应症是什么？同靶点有哪些临床阳性结果？海内外指南是否有推荐该靶点用于目标适应症的治疗？
```

**提示词**

```text
null
```

---

### 章节 3: 市场潜力分析

| 字段 | 值 |
|---|---|
| `名称` | `市场潜力分析` |
| `提示词` | 见下方代码块 |
| `doSectionSummary` | `null` |
| `sectionSummaryPrompt` | `null` |

#### 章节提示词

```text

```

#### 问题列表（1）

##### 问题 3.1: 专利分析

| 字段 | 值 |
|---|---|
| `ID` | `b8301ccd-0b24-44f4-a753-bf97ab55842e` |
| `模版ID` | `inner_bd_quick_cn` |
| `章节` | `市场潜力分析` |
| `标题` | `专利分析` |
| `场景` | `null` |
| `创建时间` | `1748248843685` |
| `更新时间` | `1748248843685` |
| `是否联网` | `True` |

**数据源**

| 字段 | 值 |
|---|---|
| `自定义数据源ID` | `` |
| `原始文档` | `null` |
| `来源类型` | `知识库` |

- `数据源文档列表`:
  - 空列表

**内容**

```text
标的产品在中国有哪些专利，请列出专利号？专利状态是获批、失效还是实审中？专利类型是否为化合物专利或用途专利？专利到期时间？标的产品是否存在公开的专利侵权问题。
```

**提示词**

```text

```

---

### 章节 4: 商业价值分析

| 字段 | 值 |
|---|---|
| `名称` | `商业价值分析` |
| `提示词` | 见下方代码块 |
| `doSectionSummary` | `null` |
| `sectionSummaryPrompt` | `null` |

#### 章节提示词

```text

```

#### 问题列表（1）

##### 问题 4.1: 上市时间预估

| 字段 | 值 |
|---|---|
| `ID` | `09eff10f-8858-4be1-8366-5f6c56d9853f` |
| `模版ID` | `inner_bd_quick_cn` |
| `章节` | `商业价值分析` |
| `标题` | `上市时间预估` |
| `场景` | `null` |
| `创建时间` | `1748248843685` |
| `更新时间` | `1748248843685` |
| `是否联网` | `True` |

**数据源**

| 字段 | 值 |
|---|---|
| `自定义数据源ID` | `` |
| `原始文档` | `null` |
| `来源类型` | `知识库` |

- `数据源文档列表`:
  - 空列表

**内容**

```text
如果现在开始在中国开始开发，预计什么时候可以在中国上市？
```

**提示词**

```text

```

---

### 章节 5: 风险提示

| 字段 | 值 |
|---|---|
| `名称` | `风险提示` |
| `提示词` | 见下方代码块 |
| `doSectionSummary` | `null` |
| `sectionSummaryPrompt` | `null` |

#### 章节提示词

```text

```

#### 问题列表（1）

##### 问题 5.1: 市场规模风险

| 字段 | 值 |
|---|---|
| `ID` | `b21f40c1-dd3d-4cf5-8b75-d1b70b82a274` |
| `模版ID` | `inner_bd_quick_cn` |
| `章节` | `风险提示` |
| `标题` | `市场规模风险` |
| `场景` | `null` |
| `创建时间` | `1748248843685` |
| `更新时间` | `1748248843685` |
| `是否联网` | `True` |

**数据源**

| 字段 | 值 |
|---|---|
| `自定义数据源ID` | `` |
| `原始文档` | `null` |
| `来源类型` | `知识库` |

- `数据源文档列表`:
  - 空列表

**内容**

```text
是否存在：会治疗的患病人数不超过1万的情况，或国内现有疗法销售额不超过1000万的情况？
```

**提示词**

```text

```

---
