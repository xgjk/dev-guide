# 基础服务 — 通用约定

本文件定义基础服务（用户、文件）所有 Open API 的通用基础信息。

## 修订记录

| 版本 | 日期 | 变更摘要 | 变更人 |
|------|------|----------|--------|
| 1.0 | 2026-03-25 | 初始版本 | 成伟 |
| 1.1 | 2026-04-13 | 独立为通用约定文件 | AI助手 |

---

## 一、访问地址
```
https://{域名}/open-api/{接口地址}
```

## 二、环境信息

| 环境 | 域名/Base URL | 备注 |
| ---- | ------------------------------ | --- |
| 生产环境 | `https://sg-al-cwork-web.mediportal.com.cn` | - |

## 三、公共请求头

| Header | 说明 | 是否必填 |
| -------- | -------------------------- | ----- |
| `appKey` | 从工作协同系统获取的 API 密钥 | 是 |

## 四、通用响应结构

所有接口响应均返回统一的 `Result<T>` 结构：

```json
{
  "resultCode": 1,
  "resultMsg": null,
  "data": null
}
```

| 字段 | 类型 | 说明 |
| ------------ | ------- | ----------------------------------------- |
| `resultCode` | Integer | 业务状态码，`1` 表示成功，非 `1` 表示失败 |
| `resultMsg`  | String  | 提示信息，成功时为 `null`，失败时为错误描述 |
| `data`       | T       | 业务数据，根据各接口不同，失败时通常为 `null` |

## 五、认证
所有接口在 Header 携带合法的 `appKey` 进行鉴权。

## 六、错误码分布
通用成功（1）和失败（0）。详细错误码见总纲文档 [基础服务API说明.md](../基础服务API说明.md) 的错误码说明章节。
